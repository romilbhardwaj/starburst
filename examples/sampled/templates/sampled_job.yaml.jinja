apiVersion: batch/v1
kind: Job
metadata:
  name: {{job}}
spec: 
  template: 
    spec: 
      containers: 
      - name: sleep
        image: alpine:latest
        imagePullPolicy: Always
        command: ["sleep", "{{time}}"]