apiVersion: batch/v1
kind: Job
metadata:
  name: sleep-{{job}}
spec: 
  template: 
    spec: 
      containers: 
      - name: sleep
        image: alpine:latest
        command: ["sleep", "{{time}}"]